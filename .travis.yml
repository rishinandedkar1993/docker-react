language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t rishinandedkar/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true rishinandedkar/docker-react npm run test